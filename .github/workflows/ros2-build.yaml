name: ROS 2 Package Build
on:
  push:
    paths:
      - '**'
      - '!**.md'
      - '!**.rst'
      - '!.github/**'
      - '.github/workflows/ros2-build.yaml'
  workflow_dispatch:

env:
  # do not use {{ github.workspace }} because it is not available in the runner
  # https://github.com/actions/runner/issues/2058
  CCACHE_DIR: $GITHUB_WORKSPACE/.ccache
  CCACHE_BASEDIR: $GITHUB_WORKSPACE
  CCACHE_NOHASHDIR: "true"
  CCACHE_COMPRESS: "true"
  CCACHE_COMPRESSLEVEL: "1"
  CCACHE_MAXSIZE: "2G"
  CCACHE_SLOPPINESS: "file_macro,time_macros,include_file_mtime,include_file_ctime,file_stat_matches,system_headers"
  CCACHE_LOGFILE: $GITHUB_WORKSPACE/ccache.log
  CCACHE_DEBUG: "1"

jobs:
  build-jazzy:
    name: Build (Jazzy)
    runs-on: ubuntu-24.04
    container:
      image: osrf/ros:jazzy-desktop-full
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          path: $GITHUB_WORKSPACE/ros_ws/src/target_package
      - uses: ros-tooling/setup-ros@v0.7
        with:
          required-ros-distributions: jazzy
      - name: Prepare ccache directory
        run: |
          mkdir -p ${{ env.CCACHE_DIR }}
      - uses: actions/cache@v4
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-jazzy-${{ hashFiles('**/CMakeLists.txt', '**/package.xml', '**/src/**/*.cpp', '**/src/**/*.hpp') }}
          restore-keys: |
            ccache-jazzy-
      - name: Install ccache
        run: |
          apt-get update
          apt-get install -y ccache
      - name: Configure ccache
        run: |
          ccache --max-size=${{ env.CCACHE_MAXSIZE }}
          ccache --set-config=compression=${{ env.CCACHE_COMPRESS }}
          ccache --set-config=compression_level=${{ env.CCACHE_COMPRESSLEVEL }}
          ccache --set-config=hash_dir=false
          ccache --set-config=compiler_check=content
          ccache --set-config=sloppiness=${{ env.CCACHE_SLOPPINESS }}
          ccache -vs
          ccache -x
          ccache -p
      - name: Search packages in this repository
        id: list_packages
        run: |
          echo "package_list=$(colcon list --names-only | grep -v 'workspace root' | sed -e ':loop; N; $!b loop; s/\n/ /g')" >> $GITHUB_OUTPUT
      - name: Show target packages
        run: |
          echo "Target packages: ${{ steps.list_packages.outputs.package_list }}"
      - name: Show CMake args
        id: cmake_args
        run: |
          echo "args=-DCMAKE_C_COMPILER_LAUNCHER=ccache -DCMAKE_CXX_COMPILER_LAUNCHER=ccache -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_FLAGS=\"-fdebug-prefix-map=${GITHUB_WORKSPACE}=.\" -DCMAKE_CXX_FLAGS=\"-fdebug-prefix-map=${GITHUB_WORKSPACE}=.\"" >> $GITHUB_OUTPUT
      - uses: Tiryoh/action-ros-ci@b2033b3
        with:
          package-name: ${{ steps.list_packages.outputs.package_list }}
          target-ros2-distro: jazzy
          no-symlink-install: true
          import-token: ${{ secrets.GITHUB_TOKEN }}
          extra-cmake-args: ${{ steps.cmake_args.outputs.args }}
      - name: Show ccache stats
        run: |
          ccache -vs
          ccache -x
          ccache -p
      - name: Show ccache log
        run: |
          cat ${{ env.CCACHE_LOGFILE }} || echo "No ccache log file found"
